import 'package:flutter/material.dart';

abstract final class AppColors {
  /// Verde salvia del borde del logo — botones y acentos principales.
  static const primary = Color(0xFF94B46C);

  static const primaryDark = Color(0xFF6E8A52);

  /// Crema del fondo del logo.
  static const surface = Color(0xFFF9F1E1);

  static const card = Color(0xFFFFFCF7);

  /// Carbón de los contornos del logo.
  static const textPrimary = Color(0xFF222222);

  static const textSecondary = Color(0xFF5C6356);

  static const border = Color(0xFFD8E0CC);

  /// Verde de los ojos del gato — acentos secundarios.
  static const accent = Color(0xFF4E7A27);
}
